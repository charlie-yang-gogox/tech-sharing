# Speaker notes — 2026-09 · 從 Prompt 到 Graph Engineering

**The deck is the home for every chapter's script.** Each content slide carries
its own `<details class="notes">` block in `index.html` — read it there and edit
it there. This file is the index that says which slide holds what; it deliberately
keeps no second copy, because two copies drift.

That was settled per chapter, not all at once: CH2 (2026-09-01), CH3 (2026-09-04)
and CH1 (2026-09-08) each moved their script into the deck as the slides went
figure-first. CH4 was written straight into the deck the same way on 2026-09-08.

Notes are hidden in slide mode (`body.mode-slide details.notes { display: none }`)
and readable in scroll mode, so they are never projected.

Keep the `## SN · <slide tag> / <zh label>` heading form below so a numbering
change stays a find-and-replace. Slide numbers follow the 20-slide deck.

---

## S3 – S5 · CH1

In `index.html`, not here — moved into the deck 2026-09-08. S3 (the divider) carries
the one line it is allowed to spend; S4 (04 / THE STACK) carries the riser-by-riser
walk, the strata beat, the two colours planted for S5, and the "most tasks don't
need the top of the stack" quote the slide itself no longer prints; S5 (05 / THE TOP
RUNG) carries the re-aimed script for the single stacked figure — the upper GRAPH,
the move that opens node ②, the real-check insistence, node isolation, and the three
"only if asked" items (the three ways a loop can stop, `/ggx-pr-review-loop`'s exit
condition, where the approval gate goes).

S5's script was **re-aimed, not copied**: the version that lived here until
2026-09-08 was written for a two-panel LOOP/GRAPH slide that no longer exists, so
its left/right walkthrough, its "point at the upper node" instruction and its
節點之間互相看不到 takeaway were all rewritten against the figure that is actually
on the page. Git has the old text if it is ever wanted.

## S6 – S12 · CH2

In `index.html`, not here — each CH2 slide carries its own `<details class="notes">`
block (07 the vehicle, 08 the pattern · GGC today, 09–10 the two pattern grids,
11 dynamic workflows, 12 the wrap-up). That is where the CH2 script was put when
those slides were cut down to one figure each; duplicating it into this file would
create a second copy that nothing keeps in sync. S6 is the divider.

## S13 – S16 · CH3

In `index.html`, not here — S14 / S15 / S16 each carry their own notes block
(Typeless · herdr · /eli5). S13 is the divider.

## S17 – S19 · CH4

In `index.html`, not here — S18 (18 / WHAT MOVED) and S19 (19 / SKILLS · WORKFLOW)
each carry their own notes block; S17 is the divider. Written 2026-09-08.

S18's notes hold everything the chapter deliberately keeps off the slide: the
「零件沒動，動的是接線」 beat with its two figures (14 → 14 agent definitions,
8 / 494 commits), every speed multiplier and why the split demonstrates nothing,
the real fan-out width (2–3, not a dozen), and the precise version of each
migration's trigger — including the spawn-depth direction, which gets corrected
on the spot by anyone who has read ARCHITECTURE R1–R5 if stated loosely.

S19's cost row says the human cost of a skill chain is **distraction, not error
accumulation** — corrected 2026-09-09 after the speaker challenged it; the docs'
own name for it is the kitchen sink session. S19 also carries a scroll-only source panel (hidden in slide mode) that puts every
claim on the page next to the sentence it came from — read it there before editing
any of that wording. S19's notes hold the same sentences verbatim (the slide only states them
in the deck's own voice and credits them in the cite line, so these are the answer
if someone challenges where the distinction comes from), the
vendor's own "only add complexity when it demonstrably improves outcomes" turned
on our null speed result, the two comparison rows that left the table (併發,
脈絡), our gate mechanics (`ggx-chain` splits the graph at its gates — to be
given ONLY if someone asks how we do it), and the speaker's full original closing
wording, verbatim, with the reason only its last two sentences are on stage.
